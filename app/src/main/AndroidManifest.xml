<?xml version="1.0" encoding="utf-8"?>
<!-- AndroidManifest.xml -->
<!-- Android 通知应用清单文件 -->
<!-- 创建日期：2026-05-14 | 作者：Cline -->
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- ===== 必需权限 ===== -->
    <!-- 通知权限（Android 13+ 必需） -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <!-- 开机启动（恢复固定通知） -->
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />
    <!-- 防止手机休眠（前台服务保活） -->
    <uses-permission android:name="android.permission.WAKE_LOCK" />
    <!-- 前台服务权限 -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <!-- Android 14+ 前台服务类型 -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_SPECIAL_USE" />

    <!-- ===== 可选权限 ===== -->
    <!-- 精确闹钟权限（定时通知） -->
    <uses-permission android:name="android.permission.SCHEDULE_EXACT_ALARM" />
    <!-- 使用精确闹钟（Android 13+ 替代权限） -->
    <uses-permission android:name="android.permission.USE_EXACT_ALARM" />

    <application
        android:name=".NotifyApp"
        android:allowBackup="true"
        android:icon="@drawable/ic_notification"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/Theme.AndroidNotify"
        tools:targetApi="34">

        <!-- ===== Activity 声明 ===== -->

        <!-- 主 Activity：首页/历史/设置 -->
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:windowSoftInputMode="adjustResize">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- 编辑通知 Activity：从通知栏编辑按钮启动 -->
        <activity
            android:name=".EditNotificationActivity"
            android:exported="false"
            android:windowSoftInputMode="adjustResize" />

        <!-- ===== Service 声明 ===== -->

        <!-- 前台保活服务：保持通知显示 + 巡检恢复 -->
        <service
            android:name=".service.NotifyForegroundService"
            android:enabled="true"
            android:exported="false"
            android:foregroundServiceType="specialUse">
            <property
                android:name="android.app.PROPERTY_SPECIAL_USE_FGS_SUBTYPE"
                android:value="Keep notifications visible and restore pinned notifications" />
        </service>

        <!-- ===== Receiver 声明 ===== -->

        <!-- 开机自启接收器 -->
        <receiver
            android:name=".receiver.BootReceiver"
            android:directBootAware="true"
            android:enabled="true"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
                <action android:name="android.intent.action.LOCKED_BOOT_COMPLETED" />
                <action android:name="android.intent.action.QUICKBOOT_POWERON" />
            </intent-filter>
        </receiver>

        <!-- 通知操作按钮接收器（复制、取消固定） -->
        <receiver
            android:name=".receiver.NotificationActionReceiver"
            android:enabled="true"
            android:exported="false" />

        <!-- 通知被划掉时的恢复接收器 -->
        <receiver
            android:name=".receiver.NotificationDismissReceiver"
            android:enabled="true"
            android:exported="false" />

        <!-- 定时通知触发接收器 -->
        <receiver
            android:name=".receiver.AlarmReceiver"
            android:enabled="true"
            android:exported="false" />

    </application>
</manifest>
